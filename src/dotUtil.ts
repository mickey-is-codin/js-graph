import { Graph, GraphvizConfig } from './types';

const graphvizHeader = '';
const graphvizFooter = '';

export const toGraphvizString = (graph: Graph, config: GraphvizConfig): string => {
  return '';
};